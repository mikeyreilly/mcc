typedef struct _IO_FILE FILE;

extern int printf (const char * __format, ...);

extern int fscanf (FILE * __stream,
     const char * __format, ...) ;

extern int fscanf (FILE * __stream,
     const char * __format, ...) ;

int main(void) {
    printf("hello world");
    return 0;
}
